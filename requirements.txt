# Python >= 3.6.8 (ensure your environment uses the correct Python version)
tensorflow>=1.12.0
# or, if you prefer PyTorch, comment out TensorFlow and uncomment the next line:
# torch>=1.1.0
cython>=0.29.2
numpy>=1.14.3
scipy>=1.1.0
scikit-learn>=0.19.1
pyyaml>=3.12

# The following are system dependencies, not installable via pip:
# g++ >= 5.4.0
# openmp >= 4.0